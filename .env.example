# Environment Configuration for QuikDB Leaderboard API

# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================
# These MUST be set before the application starts

# MongoDB Connection (REQUIRED)
# Shared database with Device API - READ-ONLY access to heartbeats
MONGODB_URI=mongodb://localhost:27017/quikdb

# Server Port (REQUIRED)
PORT=3001

# Node Environment (REQUIRED)
# Options: development, production, test
NODE_ENV=development

# API Key (REQUIRED)
# Authentication key for API access
# Clients must provide this in X-API-Key header or api_key query parameter
API_KEY=your-secure-api-key-here

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================
# These have defaults but can be overridden

# CORS Configuration
# Comma-separated list of allowed origins
# Default: https://nodes.quikdb.com,http://localhost:3000
CORS_ALLOWED_ORIGINS=https://nodes.quikdb.com,https://www.nodes.quikdb.com,https://docs.quikdb.com,http://localhost:3000,http://localhost:3001

# Rate Limiting
# Maximum requests per minute per IP
# Default: 200
RATE_LIMIT_MAX=200

# Leaderboard Service Configuration
# How often to recalculate leaderboard (milliseconds)
# Default: 60000 (1 minute)
LEADERBOARD_UPDATE_INTERVAL_MS=60000

# ============================================
# PRODUCTION EXAMPLE
# ============================================
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/quikdb?retryWrites=true&w=majority
# NODE_ENV=production
# PORT=3001
# CORS_ALLOWED_ORIGINS=https://nodes.quikdb.com,https://www.nodes.quikdb.com
# RATE_LIMIT_MAX=500
# LEADERBOARD_UPDATE_INTERVAL_MS=60000
